<!DOCTYPE html>
<html lang="it">
<head>
<meta charset="UTF-8">
<title>DJ JAGUARO FESTIVAL</title>

<!-- FONT DA FESTIVAL -->
<link href="https://fonts.googleapis.com/css2?family=Bungee&display=swap" rel="stylesheet">

<style>
  body {
    background: radial-gradient(circle at center, #000 0%, #111 60%, #000 100%);
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
    perspective: 1200px;
  }

  .box {
    width: 400px;
    height: 400px;
    border-radius: 25px;
    background: linear-gradient(270deg,
      red, orange, yellow, lime, cyan, blue, violet, red);
    background-size: 600% 600%;
    animation: rainbowBG 6s linear infinite;
    display: flex;
    justify-content: center;
    align-items: center;
    transform-style: preserve-3d;
    animation: rainbowBG 6s linear infinite, boxRotate 10s linear infinite;
    box-shadow:
      0 0 40px rgba(255,255,255,0.6),
      inset 0 0 30px rgba(255,255,255,0.3);
  }

  .text {
    font-family: 'Bungee', cursive;
    font-size: 46px;
    text-align: center;
    letter-spacing: 4px;
    background: linear-gradient(270deg,
      red, orange, yellow, lime, cyan, blue, violet, red);
    background-size: 600% 600%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    animation:
      rainbowText 4s linear infinite,
      heartbeat 1.1s infinite;
    transform: translateZ(50px);
    text-shadow:
      0 4px 0 rgba(0,0,0,0.4),
      0 8px 15px rgba(0,0,0,0.6);
  }

  @keyframes rainbowBG {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  @keyframes rainbowText {
    0% { background-position: 0% 50%; }
    100% { background-position: 100% 50%; }
  }

  @keyframes heartbeat {
    0% { transform: translateZ(50px) scale(1); }
    25% { transform: translateZ(50px) scale(1.12); }
    40% { transform: translateZ(50px) scale(1); }
    60% { transform: translateZ(50px) scale(1.18); }
    100% { transform: translateZ(50px) scale(1); }
  }

  @keyframes boxRotate {
    0% { transform: rotateY(0deg) rotateX(0deg); }
    50% { transform: rotateY(180deg) rotateX(10deg); }
    100% { transform: rotateY(360deg) rotateX(0deg); }
  }
</style>
</head>
<body>

  <div class="box">
    <div class="text">DJ<br>JAGUARO</div>
  </div>

</body>
</html>
